mutation UpdateJournal($journalGLId: UUID!) {
  updateJournal(id: $journalGLId, input: { description: "_The_ ledger. The only one." }) {
    journalId
    description
    history(first: 2) {
      nodes {
        version
        description
      }
    }
  }
}
