query transactions {
  transactions(
    index: {name: CUSTOM, sort: ASC}
    where: {custom: {index: "classification", partition: {alias: "classification", value: {eq: "internal"}}}}
    first: 10
  ) {
    nodes {
      transactionId
      tranCodeId
      effective
      metadata
    }
  }
}
