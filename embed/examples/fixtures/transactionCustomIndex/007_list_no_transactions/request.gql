query transactions {
  transactions(
    index: {name: CUSTOM, sort: ASC}
    where: {custom: {index: "classification", partition: {alias: "classification", value: {eq: "none"}}}}
    first: 10
  ) {
    nodes {
      transactionId
      tranCodeId
      effective
      metadata
    }
  }
}
