query accountSets {
  accountSets(
    index: {name: CUSTOM, sort: ASC}
    where: {custom: {index: "priority", partition: {alias: "priority", value: {eq: "none"}}}}
    first: 10
  ) {
    nodes {
      accountSetId
      name
      metadata
    }
  }
}
