Create a transaction code with a unique ID, code, description, and required parameters. Set the transaction journal ID using the provided expression, and specify the effective date and metadata. Define entries for the transfer, including account IDs, units, currency, entry type, direction, and layer. Add custom metadata with a "tier" field set to "banking". Return the transaction code ID and metadata.