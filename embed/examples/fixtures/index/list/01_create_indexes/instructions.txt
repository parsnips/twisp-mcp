Create seven indexes with specified input data:

1. Create an index named "alpha" on Account, with a partition on "alpha".
2. Create an index named "beta" on Account, with a partition on "beta" and sorted by "grade" in ascending order.
3. Create an index named "gamma" on Account, sorted by "grade" in ascending order.
4. Create an index named "priority" on AccountSet, with a partition on "priority".
5. Create an index named "tier" on TranCode, with a partition on "tier".
6. Create an index named "classification" on Transaction, with a partition on "classification" and a constraint "1 + 1 == 2".
7. Create an index named "effective" on Transaction, sorted by "effective" in ascending order.

Return the names of the newly created indexes.