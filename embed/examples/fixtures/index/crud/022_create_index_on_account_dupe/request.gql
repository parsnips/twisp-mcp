mutation createIndex($input: CreateIndexInput!) {
  schema {
    createIndex(input: $input) {
      name
      range {
        alias
        value
        sort
      }
      partition {
        alias
        value
      }
      on
      constraints
      unique
    }
  }
}
