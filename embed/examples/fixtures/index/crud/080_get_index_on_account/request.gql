query getIndex($name: String!, $on: IndexOnEnum!) {
  schema {
    index(name: $name, on: $on) {
      name
      range {
        alias
        value
        sort
      }
      partition {
        alias
        value
      }
      on
      constraints
      unique
    }
  }
}
