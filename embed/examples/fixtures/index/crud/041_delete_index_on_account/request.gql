mutation deleteIndex($name: String!, $on: IndexOnEnum!) {
  schema {
    deleteIndex(name: $name, on: $on) {
      name
      range {
        alias
        value
        sort
      }
      partition {
        alias
        value
      }
      on
      constraints
      unique
    }
  }
}
