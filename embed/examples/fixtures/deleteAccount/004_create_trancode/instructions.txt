Create a transaction code for a book transfer with the provided tranCodeId and various parameters. The transaction has a journalId expression, effective date, and metadata. The entries include two accounts: destination and source, with unit amounts, currency, entry types, directions, and layers. Add metadata to the transaction code with a tier key. Return the tranCodeId and metadata.