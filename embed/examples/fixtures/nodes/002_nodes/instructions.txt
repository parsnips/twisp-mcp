1. Query a specific account node using its base64 encoded ID. Retrieve the id, accountId, name, and description.
2. Query a specific account set node using its base64 encoded ID. Retrieve the id, accountSetId, name, and description.
3. Query a specific client node using its base64 encoded ID. Retrieve the id, principal, name, and policies including effect, actions, and resources.
4. Query a specific balance node in USD using its base64 encoded ID. Retrieve the id, accountId, journalId, currency, and settled drBalance with units and currency.
5. Query a specific index node using its base64 encoded ID. Retrieve the id, name, on, partition alias, and range alias.
6. Query a specific journal node using its base64 encoded ID. Retrieve the id, journalId, name, and description.
7. Query a specific transaction code node using its base64 encoded ID. Retrieve the id, tranCodeId, status, description, and code.
8. Query a specific transaction node using its base64 encoded ID. Retrieve the id, transactionId, and effective date.