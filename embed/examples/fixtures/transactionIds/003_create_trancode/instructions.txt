Create a transaction code with the given tranCodeId for a book transfer. Include parameters for source account ID, destination account ID, transaction classification, amount, and effective date. Set the journal ID using an expression variable, and use the given metadata for classification. Create two entries, one for the transfer in and one for the transfer out, with the specified properties. Add metadata with a "tier" key set to "banking." Return the tranCodeId and metadata.