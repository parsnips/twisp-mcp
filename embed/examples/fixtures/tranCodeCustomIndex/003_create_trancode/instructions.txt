Create a TranCode with a unique ID for a book transfer. Include parameters for source account ID, destination account ID, transaction classification, transfer amount, and effective date. Specify the transaction journal ID using an expression variable. Define two entries for both destination and source accounts, setting the appropriate units, currency, entry type, direction, and layer. Add metadata with a tier of "banking". Return the tranCodeId and metadata.