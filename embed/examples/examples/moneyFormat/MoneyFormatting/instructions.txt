1. Create a journal with the given journalId, name "GL" and description "General Ledger".
2. Create two accounts with given accountIds, codes, names, descriptions, normalBalanceType, and status.
3. Create a transaction code with the given tranCodeId, code, description, params, transaction, and entries.
4. Post transactions to test: 
   - whole number formatting
   - minimum digits formatting
   - maximum digits formatting
   - rounding with different modes
   - different locales
   - grouping
   - currency minimum digits
   - currency display
   For each test, provide a separate transaction with the given transactionId, tranCode, and params. In the response, for each test, retrieve the first entry and format the amount according to the test requirements.